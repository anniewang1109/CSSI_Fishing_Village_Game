runtime: python27
api_version: 1
threadsafe: yes

handlers:
- url: /static
  static_dir: static
#static directories should go ABOVE the catch all url (.*)
- url: .*
  script: Main.app


libraries:
- name: webapp2
  version: "2.5.2"
- name: jinja2
  version: latest
